# Sorter App

This is a project from a course I took to learn TypeScript. IT basically just shows good design patterns on TS.

### How to run

To run, type in the following command in the terminal:

```bash
npm run start
```
